import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  // The template gives each page its own tab title while keeping the product
  // name visible: "Dashboard · Spend & Saving Analytics".
  title: {
    default: "Spend & Saving Analytics",
    template: "%s · Spend & Saving Analytics",
  },
  description:
    "Analyse organisational spend, monitor budgets and savings, and manage spend records.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
